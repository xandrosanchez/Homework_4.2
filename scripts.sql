SELECT * FROM student
WHERE age BETWEEN 10 and 20;

SELECT name FROM student;

SELECT * FROM student
WHERE name LIKE ('%lex%');

SELECT * FROM student
WHERE age < student.id;

SELECT * FROM student
ORDER BY age;

